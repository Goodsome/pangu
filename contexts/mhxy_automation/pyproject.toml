[project]
name = "mhxy-automation"
version = "0.1.0"
description = ""
requires-python = ">=3.14"
dependencies = [
    "mhxy-client",
    "typer>=0.26.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mhxy_automation"]

[tool.pyright]
venvPath = "../../"
venv = ".venv"
include = ["src"]
extraPaths = ["src"]
reportUnusedCallResult = "none"

[tool.uv.sources]
mhxy-client = { workspace = true }
